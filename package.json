{
  "name": "discord-jabber-transport-bot",
  "version": "1.0.0",
  "description": "Simple transport bot between Discord channel and Jabber conference",
  "main": "app.js",
  "scripts": {
    "start": "DEBUG=* nodejs app.js",
    "start-win": "set DEBUG=info,error,debug,debug:discord,debug:jabber && node ./app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "shtrih",
  "license": "MIT",
  "dependencies": {
    "debug": "^2.2.0",
    "discord.io": "^1.12.0",
    "json-config": "0.0.1",
    "node-xmpp-client": "^3.0.1"
  },
  "engines": {
    "node": ">=0.10"
  }
}
